name: Deploy Frontend to AWS (CDK)

on:
  push:
    branches: [main]
    paths:
      - 'client/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:

# Ensure only one deployment runs at a time
concurrency:
  group: frontend-deployment
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: ./client
        run: npm ci

      - name: Build Next.js (standalone mode)
        working-directory: ./client
        env:
          NODE_ENV: production
          # Add required env vars for build (these may be needed during build)
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
          NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
        run: npm run build

      - name: Install CDK dependencies
        working-directory: ./client/infra
        run: npm install

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Validate required secrets
        run: |
          if [ -z "${{ secrets.AWS_ACCOUNT_ID }}" ]; then
            echo "❌ Error: AWS_ACCOUNT_ID secret is required"
            exit 1
          fi
          if [ -z "${{ secrets.AWS_ACCESS_KEY_ID }}" ]; then
            echo "❌ Error: AWS_ACCESS_KEY_ID secret is required"
            exit 1
          fi
          if [ -z "${{ secrets.AWS_SECRET_ACCESS_KEY }}" ]; then
            echo "❌ Error: AWS_SECRET_ACCESS_KEY secret is required"
            exit 1
          fi
          echo "✅ All required secrets are present"
        working-directory: ./client/infra

      - name: Ensure CDK Bootstrap is configured
        working-directory: ./client/infra
        env:
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
          AWS_REGION: us-east-1
          BOOTSTRAP_BUCKET: cdk-hnb659fds-assets-${{ secrets.AWS_ACCOUNT_ID }}-us-east-1
          CDK_TOOLKIT_STACK: CDKToolkit
        run: |
          set -e
          
          echo "🔍 Checking CDK bootstrap status..."
          echo "   Account: $AWS_ACCOUNT_ID"
          echo "   Region: $AWS_REGION"
          echo "   Expected bucket: $BOOTSTRAP_BUCKET"
          
          # Check if CDKToolkit CloudFormation stack exists
          echo ""
          echo "📋 Step 1: Checking for CDKToolkit CloudFormation stack..."
          if aws cloudformation describe-stacks \
            --stack-name "$CDK_TOOLKIT_STACK" \
            --region "$AWS_REGION" \
            >/dev/null 2>&1; then
            echo "   ✅ CDKToolkit stack exists"
            STACK_EXISTS=true
          else
            echo "   ❌ CDKToolkit stack not found"
            STACK_EXISTS=false
          fi
          
          # Check if bootstrap S3 bucket exists
          echo ""
          echo "🪣 Step 2: Checking for bootstrap S3 bucket..."
          if aws s3api head-bucket \
            --bucket "$BOOTSTRAP_BUCKET" \
            --region "$AWS_REGION" \
            >/dev/null 2>&1; then
            echo "   ✅ Bootstrap bucket exists: $BOOTSTRAP_BUCKET"
            BUCKET_EXISTS=true
          else
            echo "   ❌ Bootstrap bucket not found: $BOOTSTRAP_BUCKET"
            BUCKET_EXISTS=false
          fi
          
          # Determine if bootstrap is needed
          echo ""
          if [ "$STACK_EXISTS" = true ] && [ "$BUCKET_EXISTS" = true ]; then
            echo "✅ CDK bootstrap is already configured"
            echo "   Both CDKToolkit stack and bootstrap bucket exist"
            echo "   Skipping bootstrap step"
          else
            if [ "$STACK_EXISTS" = false ]; then
              echo "⚠️  CDKToolkit stack is missing - bootstrap required"
            fi
            if [ "$BUCKET_EXISTS" = false ]; then
              echo "⚠️  Bootstrap bucket was deleted - bootstrap required"
            fi
            
            echo ""
            echo "🚀 Running CDK bootstrap..."
            echo "   This will create/update the CDKToolkit stack and bootstrap bucket"
            npx cdk bootstrap \
              aws://$AWS_ACCOUNT_ID/$AWS_REGION \
              --require-approval never \
              --output-file /tmp/bootstrap-output.json || {
              echo "❌ Bootstrap failed!"
              exit 1
            }
            
            echo ""
            echo "✅ CDK bootstrap completed successfully"
            
            # Verify bootstrap after completion
            echo ""
            echo "🔍 Verifying bootstrap configuration..."
            if aws cloudformation describe-stacks \
              --stack-name "$CDK_TOOLKIT_STACK" \
              --region "$AWS_REGION" \
              >/dev/null 2>&1; then
              echo "   ✅ CDKToolkit stack verified"
            else
              echo "   ⚠️  Warning: CDKToolkit stack verification failed"
            fi
            
            if aws s3api head-bucket \
              --bucket "$BOOTSTRAP_BUCKET" \
              --region "$AWS_REGION" \
              >/dev/null 2>&1; then
              echo "   ✅ Bootstrap bucket verified: $BOOTSTRAP_BUCKET"
            else
              echo "   ⚠️  Warning: Bootstrap bucket verification failed"
            fi
          fi
          
          echo ""
          echo "✅ CDK bootstrap check complete"

      - name: Deploy infrastructure with CDK
        working-directory: ./client/infra
        env:
          CDK_DEFAULT_ACCOUNT: ${{ secrets.AWS_ACCOUNT_ID }}
          CDK_DEFAULT_REGION: us-east-1
          S3_BUCKET_NAME: ${{ vars.S3_BUCKET_NAME }}
          ROUTE53_HOSTED_ZONE_ID: ${{ secrets.ROUTE53_HOSTED_ZONE_ID }}
          COLYSEUS_DOMAIN: ${{ vars.COLYSEUS_DOMAIN }}
          COLYSEUS_HOST_IP: ${{ vars.COLYSEUS_HOST_IP }}
        run: npx cdk deploy --all --require-approval never --context account=${{ secrets.AWS_ACCOUNT_ID }} --context region=us-east-1

      - name: Output deployment URLs
        run: |
          echo "✅ Infrastructure deployed successfully!"
          echo "Check AWS Console for deployed resources"
